const num1 = 5
const num2 = 10

function addValues() {
    console.log(`the sum value is ${num1 + num2}`);
}

addValues();

//when we put a function inside a module i.e. addValue(), the code will run without variable i.e. require('./7.mind-grenade') instead of const sum = require('./7.mind-grenade